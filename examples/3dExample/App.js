import React from 'react';
import { Canvas } from 'react-canvas';

const App = ({}) => {
	return (<div>
		<Canvas
			width={400}
			height={300}
            enable3d={true}
		>

		</Canvas>
	</div>);
};

export default App;